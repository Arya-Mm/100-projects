# Project 01 — Hello CLI Toolkit

A simple, professional-grade Python CLI toolkit built using `click`.

## 🚀 Features
- Subcommands (`hello`, `config`)
- Config file support (`config.json`)
- Installable via `pip`
- Testable with `pytest`
- Easy to extend

---

## 📦 Installation

Run this in the project folder:

```bash
pip install -e .
